{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf build/ && rimraf dist/ && rimraf .cache/",
    "start": "parcel public/index.html",
    "watch": "parcel watch public/index.html --out-dir build/ --public-url / --no-source-maps",
    "build": "parcel build public/index.html --out-dir build/ --public-url / --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/react": "^16.9.46",
    "@types/react-dom": "^16.9.8",
    "bootstrap": "^4.5.2",
    "react": "^16.13.1",
    "react-bootstrap": "^1.3.0",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "rimraf": "^3.0.2",
    "sass": "^1.26.10",
    "scss": "^0.2.4",
    "typescript": "^3.9.7"
  }
}
